<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ClntPriceCapMapper">


<!--    @Select("SELECT ClntCode, Market, Instrument, MarginPercent, PriceCap, Description, SyncRef, SyncRefCMS, [timestamp], TTLRef, StockLimit\n" +-->
<!--    "FROM dbo.ClntPriceCap not like '%[a-zA-Z]%' escape '\\' ORDER BY ClntCode DESC OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY")-->
<!--    List<ClntPriceCapPO> selectByPage(@Param("offset") int offset, @Param("limit") int limit);-->


<!--    <select id="selectByPage" resultType="org.example.pojo.ClntPriceCapPO">-->
<!--        SELECT ClntCode, Market, Instrument, MarginPercent, PriceCap, Description, SyncRef, SyncRefCMS, [timestamp], TTLRef, StockLimit-->
<!--        FROM dbo.ClntPriceCap-->
<!--        WHERE ClntCode NOT LIKE '%[a-zA-Z]%' ESCAPE '\\'-->
<!--        ORDER BY ClntCode DESC-->
<!--    </select>-->
</mapper>
